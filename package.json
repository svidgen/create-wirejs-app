{
	"name": "wirejs-infra",
	"workspaces": [
		"packages/create-wirejs-app",
		"packages/wirejs-scripts",
		"packages/wirejs-resources",
		"packages/test-default"
	],
	"private": "true",
	"version": "1.0.0",
	"description": "wirejs-scripts and create scripts.",
	"author": "Jon Wire",
	"license": "MIT",
	"scripts": {
		"build": "npm run build --ws --if-present",
		"clean": "npm run clean --ws --if-present",
		"start": "npm run start -w test-default",
		"start-open": "open=1 npm run start -w test-default"
	},
	"devDependencies": {
		"rimraf": "^6.0.1"
	}
}
