{
	"name": "sample-app",
	"version": "2.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"src",
		"api"
	],
	"dependencies": {
		"wirejs-dom": "^1.0.27"
	},
	"devDependencies": {
		"highlight.js": "^11.5.1",
		"wirejs-scripts": "file:../../wirejs-scripts",
		"wirejs-services": "file:../../wirejs-services"
	},
	"scripts": {
		"prebuild": "npm run prebuild --workspaces --if-present",
		"prestart": "npm run prestart --workspaces --if-present",
		"start": "wirejs-scripts start",
		"build": "wirejs-scripts build"
	}
}
